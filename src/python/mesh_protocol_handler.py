import json\nimport time\nimport hashlib\nfrom typing import Dict, Any, Optional\n\nclass MeshProtocolHandler:\n    """\n    Manages the encoding, decoding, and routing of messages within the Flux Distributed JSON Mesh.\n    Handles various message types including data synchronization, peer discovery, and control signals.\n    """\n\n    PROTOCOL_VERSION = "1.0.0"\n    MESSAGE_TYPES = {\n        "DATA_SYNC": "data_sync",\n        "PEER_DISCOVERY": "peer_discovery",\n        "ACKNOWLEDGEMENT": "ack",\n        "ERROR": "error",\n        "HEARTBEAT": "heartbeat"\n    }\n    MAX_PAYLOAD_SIZE = 1024 * 1024 * 5 # 5MB\n\n    def __init__(self, node_id: str, secret_key: str):\n        """\n        Initializes the protocol handler for a specific mesh node.\n\n        Args:\n            node_id (str): Unique identifier for this mesh node.\n            secret_key (str): Shared secret for message signing/verification.\n        """\n        if not node_id or not secret_key:\n            raise ValueError("Node ID and secret key must be provided.")\n        self.node_id = node_id\n        self.secret_key = secret_key.encode('utf-8')\n\n    def _generate_signature(self, message_payload: str) -> str:\n        """Generates an HMAC-SHA256 signature for the message payload."""\n        hasher = hashlib.sha256()\n        hasher.update(message_payload.encode('utf-8'))\n        hasher.update(self.secret_key)\n        return hasher.hexdigest()\n\n    def _verify_signature(self, message_payload: str, signature: str) -> bool:\n        """Verifies the HMAC-SHA256 signature of a message."""\n        return self._generate_signature(message_payload) == signature\n\n    def encode_message(self,\n                       message_type: str,\n                       payload: Dict[str, Any],\n                       recipient_id: Optional[str] = None) -> str:\n        """\n        Encodes a dictionary payload into a standardized mesh message string.\n\n        Args:\n            message_type (str): The type of message (e.g., 'DATA_SYNC', 'PEER_DISCOVERY').\n            payload (Dict[str, Any]): The actual data payload to be sent.\n            recipient_id (Optional[str]): The intended recipient node ID, if applicable.\n\n        Returns:\n            str: The JSON-encoded message string ready for transmission.\n\n        Raises:\n            ValueError: If message_type is invalid or payload is too large.\n        """\n        if message_type not in self.MESSAGE_TYPES.values():\n            raise ValueError(f"Invalid message type: {message_type}")\n\n        message_data = {\n            "protocol_version": self.PROTOCOL_VERSION,\n            "sender_id": self.node_id,\n            "timestamp": int(time.time()),\n            "message_type": message_type,\n            "payload": payload,\n            "recipient_id": recipient_id # Optional, for direct messages\n        }\n\n        payload_str = json.dumps(message_data['payload'], sort_keys=True)\n        # Re-serialize the full message_data for consistent signing, excluding signature itself\n        message_for_signing = json.dumps({k: v for k, v in message_data.items() if k != 'signature'}, sort_keys=True)\n\n        if len(message_for_signing.encode('utf-8')) > self.MAX_PAYLOAD_SIZE:\n            raise ValueError("Message payload exceeds maximum allowed size.")\n\n        message_data["signature"] = self._generate_signature(message_for_signing)\n\n        return json.dumps(message_data)\n\n    def decode_message(self, raw_message: str) -> Optional[Dict[str, Any]]:\n        """\n        Decodes a raw message string received from the mesh and verifies its integrity.\n\n        Args:\n            raw_message (str): The raw JSON-encoded message string.\n\n        Returns:\n            Optional[Dict[str, Any]]: The decoded message dictionary if valid, otherwise None.\n        """\n        try:\n            message = json.loads(raw_message)\n        except json.JSONDecodeError:\n            print("PROTOCOL_ERROR: Received malformed JSON message.")\n            return None\n\n        required_keys = ["protocol_version", "sender_id", "timestamp", "message_type", "payload", "signature"]\n        if not all(key in message for key in required_keys):\n            print(f"PROTOCOL_ERROR: Missing required keys in message from {message.get('sender_id', 'unknown')}.")\n            return None\n\n        if message["protocol_version"] != self.PROTOCOL_VERSION:\n            print(f"PROTOCOL_ERROR: Incompatible protocol version. Expected {self.PROTOCOL_VERSION}, got {message['protocol_version']}.")\n            return None\n\n        # Re-serialize the message data (excluding the signature) to verify\n        received_signature = message.pop("signature")\n        message_for_verification = json.dumps(message, sort_keys=True)\n\n        if not self._verify_signature(message_for_verification, received_signature):\n            print(f"SECURITY_WARNING: Invalid signature for message from {message.get('sender_id', 'unknown')}.")\n            return None\n\n        # Add signature back for complete message, if needed by consumers\n        message["signature"] = received_signature\n        return message\n\n    def create_data_sync_message(self, data_updates: Dict[str, Any], target_collection: str, version: int) -> str:\n        """Helper to create a data synchronization message."""\n        payload = {\n            "updates": data_updates,\n            "collection": target_collection,\n            "version": version\n        }\n        return self.encode_message(self.MESSAGE_TYPES["DATA_SYNC"], payload)\n\n    def create_peer_discovery_message(self, known_peers: Dict[str, str]) -> str:\n        """Helper to create a peer discovery message."""\n        payload = {\n            "known_peers": known_peers # {node_id: address}\n        }\n        return self.encode_message(self.MESSAGE_TYPES["PEER_DISCOVERY"], payload)\n\n    def create_acknowledgement_message(self, original_message_id: str, status: str = "success", details: Optional[str] = None) -> str:\n        """Helper to create an acknowledgement message."""\n        payload = {\n            "original_message_id": original_message_id,\n            "status": status,\n            "details": details\n        }\n        return self.encode_message(self.MESSAGE_TYPES["ACKNOWLEDGEMENT"], payload)\n\n    def create_error_message(self, error_code: str, error_description: str, original_message_id: Optional[str] = None) -> str:\n        """Helper to create an error message."""\n        payload = {\n            "error_code": error_code,\n            "description": error_description,\n            "original_message_id": original_message_id\n        }\n        return self.encode_message(self.MESSAGE_TYPES["ERROR"], payload)\n\n# Example Usage (for testing/demonstration, not part of actual library code)\nif __name__ == "__main__":\n    node_a_id = "node-alpha-123"\n    node_a_secret = "super_secret_key_alpha"\n    node_b_id = "node-beta-456"\n    node_b_secret = "super_secret_key_beta"\n\n    handler_a = MeshProtocolHandler(node_a_id, node_a_secret)\n    handler_b = MeshProtocolHandler(node_b_id, node_b_secret) # For simulating verification from another node\n\n    # --- Test Data Sync Message ---\n    print("--- Testing Data Sync Message ---")\n    data_to_sync = {"user:123": {"name": "Alice", "email": "alice@example.com", "version": 5}}\n    sync_message = handler_a.create_data_sync_message(data_to_sync, "users", 5)\n    print(f"Encoded Data Sync Message:\n{sync_message}\n")\n\n    decoded_sync_message = handler_b.decode_message(sync_message)\n    if decoded_sync_message:\n        print(f"Decoded Data Sync Message (Node B):\n{json.dumps(decoded_sync_message, indent=2)}\n")\n        assert decoded_sync_message["sender_id"] == node_a_id\n        assert decoded_sync_message["message_type"] == "data_sync"\n        assert decoded_sync_message["payload"]["collection"] == "users"\n        print("Data Sync Message decoding and verification successful.")\n    else:\n        print("Data Sync Message decoding failed.")\n\n    # --- Test Invalid Signature ---\n    print("\n--- Testing Invalid Signature ---")\n    corrupted_sync_message = sync_message + "corrupt"\n    if not handler_b.decode_message(corrupted_sync_message):\n        print("Invalid signature correctly detected for corrupted message.")\n    else:\n        print("ERROR: Invalid signature NOT detected for corrupted message.")\n\n    # --- Test Peer Discovery Message ---\n    print("\n--- Testing Peer Discovery Message ---")\n    peers = {"node-gamma-789": "192.168.1.10:8000", "node-delta-012": "192.168.1.11:8000"}\n    discovery_message = handler_a.create_peer_discovery_message(peers)\n    print(f"Encoded Peer Discovery Message:\n{discovery_message}\n")\n\n    decoded_discovery_message = handler_b.decode_message(discovery_message)\n    if decoded_discovery_message:\n        print(f"Decoded Peer Discovery Message (Node B):\n{json.dumps(decoded_discovery_message, indent=2)}\n")\n        assert decoded_discovery_message["message_type"] == "peer_discovery"\n        assert "node-gamma-789" in decoded_discovery_message["payload"]["known_peers"]\n        print("Peer Discovery Message decoding and verification successful.")\n    else:\n        print("Peer Discovery Message decoding failed.")\n\n    # --- Test Error Message ---\n    print("\n--- Testing Error Message ---")\n    error_msg = handler_a.create_error_message("DATA_CONFLICT", "Version mismatch detected for 'user:123'", "msg-abc-123")\n    print(f"Encoded Error Message:\n{error_msg}\n")\n\n    decoded_error_msg = handler_b.decode_message(error_msg)\n    if decoded_error_msg:\n        print(f"Decoded Error Message (Node B):\n{json.dumps(decoded_error_msg, indent=2)}\n")\n        assert decoded_error_msg["message_type"] == "error"\n        assert decoded_error_msg["payload"]["error_code"] == "DATA_CONFLICT"\n        print("Error Message decoding and verification successful.")\n    else:\n        print("Error Message decoding failed.")\n